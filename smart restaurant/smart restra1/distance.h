
unsigned double float count_distance (int count)
{ 
	
	double float distance;
    count++; //increasing count
    distance =3.14*11*(count/2);  //linear conversion 8 is because there are 8 segemnt to reflect
    return(distance);
}
